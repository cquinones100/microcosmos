import Scene from "../scene";

export const create = (scene: Scene) => {
  // const createHeterotroph = () => {
  //   const organism = scene.createHeterotroph({ x: scene.center.x, y: scene.center.y });
  //   organism.geneticCode = new GeneticCode([
  //     new MovementGene(organism),
  //     new SeeksEnergy(organism),
  //   ]);

  //   organism.maxEnergy = 1000;
  //   organism.energy = organism.maxEnergy;
  // }

  // createHeterotroph();
  // createHeterotroph();

  // const autotroph = scene.createAutotroph({ x: scene.center.x + 100, y: scene.center.y + 100 });
}
